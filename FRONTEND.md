# CodexChatPane 前端实现说明

本文描述当前正式页面。`PROTOTYPE-A.html` 是保留的视觉历史，不参与构建。

## 1. 页面与渲染

正式入口是 `index.html`，加载 `src/app.js` 与 `src/styles.css`。页面使用原生 DOM 字符串渲染，无 React/Vue。所有用户可见 SVG 图标集中在 `index.html` 的 symbol 库。

`render()` 重建主要工作区；高频更新使用定向 DOM 更新：工作动画 600 ms、执行时间 1 s、Source 快照 1 s、打开的预览 3 s。

所有插入 HTML 的外部文本必须经过 `esc()`。模板 `ui` 仅处理静态文案翻译，不代替转义。

## 2. 顶层布局

```text
┌─ 自定义标题栏 ────────────────────────────────┐
├─ Projects | Chats                             ┤
│                                               │
│  当前 Tab 的单栏工作区                        │
│                                               │
├─ 状态栏：同步 | Chat 时间 | 额度               ┤
└───────────────────────────────────────────────┘
```

- 初始窗口：760×900，最小 320×240，无系统 decorations。
- 默认 Tab：Projects。
- 单栏窗口宽度保存到 `config/settings.toml`，上下栏高度保存到 `preferences-v1`。

## 3. 标题栏

与正文使用同一主题色，不模拟白色系统标题栏。包含：

- 应用图标与名称；
- 设置（打开设置页）；
- 窗口置顶，默认开启；
- 最小化、最大化/还原、关闭。

设置页替换当前 Tab 工作区，不进入标题栏下拉。包含：

- 外观：语言、亮暗模式、主题族、Tab/栏/行字号；
- 窗口：置顶、关闭时询问/托盘/退出；
- Codex MCP 操作开关；
- 工具配置导入/导出（导出为可读 TOML，保存位置自选）。

可交互控件标记为非拖动区域，其余标题栏可拖动窗口。

## 4. 统一栏目样式

Projects、Project Chats、Chats、Timeline、Archive 使用同一 `view-block`/`view-head` 体系：

- 栏目头占满宽度，使用完整语义背景、上下边界和统一高度；
- 左侧为语义图标、标题、计数和多选徽章；
- 右侧按钮按“过滤/排序 → 搜索 → 新建”排列；
- 激活按钮使用背景、边框和图标色共同表达，不只依赖颜色；
- 搜索框是栏目内绝对定位覆盖层，不增加布局行。

排序符号：

- `N↑/N↓`：名称升/降序；未高亮表示关闭；
- `P↑/P↓`：Project 升/降序；未高亮表示关闭；

## 5. Folder 与 Group 层级

Folder/Group 头使用比列表行更明显的整行背景和粗体名称。不同层级之间保留小间距，缩进按 `--depth`/`--group-depth` 递增但保持紧凑。

- 普通 Folder 使用蓝色图标。
- “无项目对话”使用紫色 Project 图标。
- PIN、ALL 固定 Group 使用绿色文字，背景沿用普通 Group。
- Group/Folder 头不使用状态色竖条；时间色条属于 Project 与 Chat 行。

固定 Group 不显示重命名、删除或拖动手柄。自建 Group 支持多级子 Group。

## 6. Project 行

结构：

```text
[时间色条] [状态槽] [Project 图标] 名称   未读数 工作数 总数
```

- 名称与 Chat 使用相同字号和字重；无项目项加粗。
- 未读红点和工作状态可同时显示；同时存在时在状态槽内缩小并上下排列。
- 仅路径无效时 Project 主图标替换为问题图标。
- 行选择、Hover、焦点和多选必须视觉可区分。无项目对话选中时同样使用选择高亮；此时新建对话创建无项目对话。

## 7. Chat 行

全局 Chats：

```text
[时间色条][状态/执行时间] Project列 标题 [预览] [归档][Pin]
```

Project Chats 与其相同，只省略 Project 列。

- Project 列按最长可见名称计算，上限 20ch，名称加粗而非标签样式。
- 时间色条是整行第一列：今天红、昨天黄、前天绿、更早蓝。
- 预览图标始终显示，位于标题之后。
- 归档按钮位于 Pin 左侧。
- Pin 图标只在已 Pin 或行 Hover/焦点时显示；已 Pin 时点击只能取消。
- 不显示常驻 Chat 气泡图标或重命名按钮；重命名在右键菜单。

## 8. 状态与动画

状态图标优先级：诊断错误 → 工作中 → 完成待查看 → 空闲。

- 未读：红色。
- 工作：绿色，多点图标按 1→2→3→1 切换。
- 完成待查看：黄色。
- 启动前已读/普通：主题蓝色或空状态。
- 归档：紫色。

动画不能只依赖 CSS animation；`updateWorkingFrame()` 切换实际图形，因此 Windows “关闭动画”时仍有状态变化。

## 9. 搜索与选择器

统一搜索组件：搜索图标点击后在栏目标题下方覆盖展开，包含搜索图标、单层外框、输入框和最右侧 `X`。无短竖线，不改变栏目高度，宽度跟随栏目。

统一 Picker 组件：

- 圆角浮层、统一阴影/边框/行高；
- 顶部小搜索框；
- 列表限制在视口内；
- 项目过滤为多选；
- 点击外部或 Escape 关闭；重复打开不能失效。

Projects 的 Chats 栏新建对话直接使用当前选中 Project。Chats Tab 弹出项目选择器，默认“无项目对话”，也可选一个 Project。

## 10. 菜单与多选

右键菜单按当前栏目提供一级动作；选择具体 Folder/Group 时打开统一 Picker 弹窗，菜单本身不显示滚动条。

- Chats：打开、改名、Pin、归档、打开项目文件夹、复制信息、移动/复制到 Group。
- Projects：在 Codex 新建对话、打开项目文件夹、复制信息、移动到 Folder。
- 归档 Chat：改名与 Pin 禁用并显示原因，恢复可用。

Ctrl 切换选择，Shift 选择范围。右键已选项时作用于当前选择；否则先把目标设为唯一选择。拖动用于移动到 Folder/Group，不承担普通列表排序。打开菜单立即隐藏预览。

## 11. 对话预览

仅悬停预览图标触发。弹层铺满标题栏以下的工具内容区，可接收鼠标滚轮；Pin 后保持显示，直到手动关闭。

内容样式：

- user：红色左边线；中途打断为蓝色左边线；
- assistant：正文；
- process：低对比度图标与描述；
- 系统上下文：黄色左边线，默认只显示类型，可展开原文；
- 不显示标题、时间和私有 reasoning。

打开后把最后一次用户输入定位到顶部。向上触顶加载历史页并保持视觉位置；最新页刷新时不抢用户滚动。

## 12. Timeline 与 Archive 下栏

Timeline/Archive 默认关闭，由栏目右下角图标打开，行为类似抽屉。打开时插入水平 resizer；栏目头和主栏目一致，但保持紧凑。

Timeline 行时间靠左、状态图标在内容最前；全局 Timeline 的 Project 名使用不同文字色。Archive 一行显示，不重复“已归档”文字。

## 13. 状态栏

中部格式：

```text
Today 20:16[12s]
2026-09-01 09:52[1m8s]
```

没有执行时间时省略方括号。状态栏不显示 Project 或 Chat 标题。

右侧额度项：

```text
5h  ◯ 65% 18:20    Week  ◯ 83% 6d12h
```

窄宽度优先保留环、百分比和紧凑重置时间。悬停显示精确本地重置时间。

## 14. 主题与语言

主题族：Catppuccin、Tokyo Night、Gruvbox Material、Everforest、Ayu；每族亮/暗两套。颜色只从 CSS token 派生，组件内不硬编码主题背景。

中英文由 `src/i18n.js` 运行时翻译静态 UI。Project、Chat、Group、错误原文等动态内容不翻译。

## 15. 可访问性

- 图标按钮必须有 `title` 和 `aria-label`。
- Tab、折叠栏、Picker、菜单维护正确 `aria-*` 状态。
- 可操作行可聚焦，并有可见 focus 样式。
- 状态不能只靠颜色；图形或文字同时表达。
- `prefers-reduced-motion` 下不使用装饰动画，但状态内容切换保留。

## 16. 前端检查

```powershell
node tests/frontend.mjs
node tests/dynamic.mjs
npm run build
```

修改交互后还应在 Tauri 运行时检查真实数据；纯 Vite 页面无法验证 SQLite、Deep Link、MCP 或对话预览读取。
